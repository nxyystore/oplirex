[package]
name = "oplirex"
version = "1.0.0"
edition = "2021"
description = "opencode.ai Ratelimit Reset + Anthropic Proxy Bridge"
authors = ["nxyy"]
license = "MIT"
repository = "https://github.com/nxyystore/oplirex"
homepage = "https://github.com/nxyystore/oplirex"
readme = "README.md"
keywords = ["opencode", "anthropic", "proxy", "warp", "rate-limit"]
categories = ["command-line-utilities", "web-programming"]

[[bin]]
name = "oplirex"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5", features = ["derive", "env"] }
clap_complete = "4.5"
colored = "2.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
anyhow = "1.0"
tokio = { version = "1", features = ["full", "process"] }
axum = { version = "0.7", features = ["json", "query"] }
reqwest = { version = "0.12", features = ["json", "stream", "gzip", "brotli", "blocking"] }
hyper = { version = "1", features = ["full"] }
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.5", features = ["trace", "cors", "fs"] }
futures = "0.3"
futures-util = "0.3"
bytes = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
url = "2.5"

[dev-dependencies]
tempfile = "3.10"

[profile.release]
strip = true
lto = true
codegen-units = 1
panic = "abort"